# Gradle
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Shared mod properties (MC-version-specific values live in each subproject's gradle.properties)
mod_version=1.2.0
maven_group=io.arona74
archives_base_name=aronalayersextras
loom_version=1.17-SNAPSHOT
